{
	"name": "app-quiz",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"private": true,
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev:back": "cd backend && php artisan serve --host=127.0.0.1 --port=8000",
		"dev:front": "cd frontend && npx vite",
		"dev": "concurrently -k -p \"[{name}]\" -n back,front -c green,cyan \"npm:dev:back\" \"npm:dev:front\"",
		"setup": "concurrently -k -p \"[{name}]\" -n root,back,front -c magenta,green,cyan \"npm install --yes\" \"cd backend && composer install --no-interaction\" \"cd frontend && npm install --yes\""
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Mathieu-2305/app-quiz.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/Mathieu-2305/app-quiz/issues"
	},
	"homepage": "https://github.com/Mathieu-2305/app-quiz#readme",
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}
